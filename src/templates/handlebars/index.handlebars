<!DOCTYPE html>
<html lang="zh">
<head>
  <meta charset="UTF-8">
  <title>handlebars example</title>
</head>
<body>
  <h1>Hello, {{name}}</h1>
  <h2>template engine: handlebars</h2>
  <ul>
    {{#each languages}}
      <li>{{this}}</li>
    {{/each}}
  </ul>
</body>
</html>
